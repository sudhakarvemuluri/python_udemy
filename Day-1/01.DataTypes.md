Python Data Types:
    Numeric Types
    Variables
    Literals
    Conversions

What is a program:
    Program is a step by step procedure for performing a particular task.
    In a program we have Data and Instructions(Operations that are performed upon data).

    
what are variables?
    Name given to the data 'length'(variable) = 15(Data)
Declaration & Initialization:
    length(Declaration) = 15(initialization)
Memory Allocation
    length - 15, some memory is allocated to data and we will give that data a name called variable length, in another word a variable is pointing or referring to a memory where the data is allocated.
Types of Data
    Integer
    Float(decimal)
    String
Multiple Declaration
    name, price, qty = 'soap', 12.75, 3
    The order which you declare the variables in the same orders the values will be initialiaed to that variable.
        a, b, c = 1, 1, 1 
        If the value is same the memory allocation will be at one location only and all the 3 variables will refer to the same memory location, python will not create different location for each value if the values are same.
        a=b=c=1 

Dynamically typed and statically typed:


data Types:
    Numeric:
        Integer
        FLoat
        bool complex
    Sequence:
        List = []
        tuple = (  )
        string = 'dq'
    Set:
        set = {1,2,3,4}  #No index, unique values, unordered
    Dictionary:
        dict 